# Netlify deployment for Next.js
# Let the plugin control publish directory so CSS and static assets load correctly.
# https://docs.netlify.com/frameworks/next-js/overview/

[build]
  command = "npm run build"

[build.environment]
  NODE_VERSION = "20"

[[plugins]]
  package = "@netlify/plugin-nextjs"
